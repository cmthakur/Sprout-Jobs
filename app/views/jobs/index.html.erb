<header><%=link_to "Post New Job", new_job_path %> </header>

<% @jobs.each do |job|%>
<h2><%= link_to "#{job.title}", job_path(job) %></h2>
<%= link_to "Edit details", edit_job_path(job) %>
<%= link_to "Remove this Job", job_path(job), :confirm =>"Are you sure?", :method => :delete %>
<br />
<% end %>
<%=link_to "Show Archived Jobs", show_archived_jobs_jobs_path %>

